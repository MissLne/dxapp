<!--components/indexPageItem/manageActivity/manageBtn/manageBtn.wxml-->
<view class="manageBtn">
  <view wx:for="{{iconfonts}}" wx:key="index" bind:tap="clickHandle" data-num="{{index}}">
    <image src="{{item.url}}" />
    <view >{{item.name}}</view>
  </view>
</view>
